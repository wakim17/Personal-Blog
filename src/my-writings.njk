---
layout: base.njk
title: My Writings
---

{% set categories = ["All", "Theology & Religion", "Health & Lifestyle", "Science", "Technology & Innovation", "History", "Theory of Knowledge", "Politics", "Philosophy", "Creative writing", "Literature"] %}

<div class="homepage-container container">
  <h1 class="section-heading">My Writings</h1>

  <div class="filter-sort-bar">
    <section class="category-filters" aria-label="Filter articles by category">
      <ul class="filter-list" id="category-filters" role="tablist" aria-label="Category filters">
        {% for category in categories %}
          <li role="presentation">
            <button class="filter-btn {% if loop.first %}active{% endif %}" role="tab" aria-selected="{% if loop.first %}true{% else %}false{% endif %}" data-category="{{ category }}">{{ category }}</button>
          </li>
        {% endfor %}
      </ul>
    </section>

    <div class="sort-controls">
      <label for="sort-order" class="sort-label">Sort:</label>
      <select id="sort-order" class="sort-select" aria-label="Sort writings by date">
        <option value="newest">Newest First</option>
        <option value="oldest">Oldest First</option>
      </select>
    </div>
  </div>

  <p id="filter-status" class="sr-only" aria-live="polite" role="status"></p>
  <section class="feed-grid" id="article-feed" aria-label="Articles list">
    {% for post in collections.articles %}
      <article class="card" data-category="{{ post.data.primary_category }}" data-date="{{ post.date.toISOString() }}">
        {% if post.data.thumbnail %}
          <div class="card-image">
            <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}">
          </div>
        {% endif %}
        <div class="card-content">
          <div class="card-meta">
            <span class="badge primary-category">{{ post.data.primary_category }}</span>
            <time datetime="{{ post.date.toISOString() }}">{{ post.data.date | postDate }}</time>
          </div>
          <h2 class="card-title"><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
          <p class="card-excerpt">{{ post.data.excerpt }}</p>
        </div>
      </article>
    {% endfor %}
  </section>
</div>
